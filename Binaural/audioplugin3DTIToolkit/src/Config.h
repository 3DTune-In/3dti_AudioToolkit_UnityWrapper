// Simple config file to determine whether to include bundled HRTF binaries

#pragma once

// Uncomment this line to exclude the binaries for a smaller build
// #define SPATIALIZER3DTI_EXCLUDE_DEFAULT_LISTENER_BINARIES
